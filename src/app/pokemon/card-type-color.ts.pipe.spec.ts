import { CardTypeColorTsPipe } from './card-type-color.ts.pipe';

describe('CardTypeColorTsPipe', () => {
  it('create an instance', () => {
    const pipe = new CardTypeColorTsPipe();
    expect(pipe).toBeTruthy();
  });
});
